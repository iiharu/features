#!/bin/sh

get_in_path_except_current() {
    which -a "$1" | grep -A1 "$0" | grep -v "$0"
}

antigravity="$(get_in_path_except_current antigravity)"

if [ -n "$antigravity" ]; then
    exec "$antigravity" "$@"
else
    echo "antigravity is not installed" >&2
    exit 127
fi
